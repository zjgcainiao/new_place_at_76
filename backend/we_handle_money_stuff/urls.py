from django.urls import path
from we_handle_money_stuff import views

app_name = "we_handle_money_stuff"

urlpatterns = [
# path('gl-journal-entries/', views.GLJournalEntryListCreateView.as_view(), name='gl-journal-entries-list-create'),
]
